# 三个核心场景投掷方式统一计划

## 当前状态

### 场景 1: dice_demo_script.gd
**投掷方式**: 通过 DiceManager 间接投掷
```gdscript
func throw_dice_with_charge():
    var charge_ratio = charge_time / max_charge_time
    dice_manager.throw_all_dice_with_charge(charge_ratio)
```

**问题**:
- 未使用 DiceThrowController
- 独立的蓄力逻辑
- 独立的震动效果实现

### 场景 2: skill_dice_test.gd
**投掷方式**: 独立的投掷方法
```gdscript
func throw_all_dices_with_charge(charge_ratio: float):
    var force_magnitude = (min_force_ratio + (charge_ratio * (1.0 - min_force_ratio))) * max_force
    for dice in all_dices:
        dice.roll(force, angular_force)
```

**问题**:
- 未使用 DiceThrowController
- 独立的力度计算公式
- 独立的蓄力逻辑

### 场景 3: attr_dice_test.gd
**投掷方式**: 简单投掷
```gdscript
func throw_dice():
    for dice in attr_dices:
        var force = Vector3(randf_range(-5, 5), randf_range(5, 10), randf_range(-10, -2))
        dice.roll(force)
```

**问题**:
- 未使用 DiceThrowController
- 无蓄力功能
- 力度参数硬编码

## 统一方案

### 步骤 1: 重构 dice_demo_script.gd

**修改内容**:
```gdscript
# _input 方法中
func _input(event):
    if event.is_action_pressed("ui_accept") and is_in_initial_state:
        is_charging = true
        DiceThrowController.start_charge()  # ✅ 使用统一控制器
        # 存储原始位置用于震动
        DiceThrowController.register_all_positions(get_all_dices())
    elif event.is_action_released("ui_accept") and is_charging:
        is_charging = false
        var dices = get_all_dices()
        DiceThrowController.end_charge(dices)  # ✅ 使用统一控制器

func _process(delta):
    if is_charging:
        var charge_ratio = DiceThrowController.update_charge(delta)  # ✅ 使用统一控制器
        # 应用震动效果
        DiceThrowController.apply_shake(get_all_dices(), original_positions, charge_ratio, delta)
```

**删除代码**:
- 删除 `charge_time` 变量
- 删除独立的蓄力计算逻辑
- 删除独立的震动效果代码

### 步骤 2: 重构 skill_dice_test.gd

**修改内容**:
```gdscript
# _input 方法中
func _input(event):
    if event.keycode == KEY_SPACE and event.pressed and is_preparation_mode:
        start_charging()
    elif event.keycode == KEY_SPACE and not event.pressed and is_charging:
        throw_all_battle_dices()

func start_charging():
    is_charging = true
    DiceThrowController.start_charge()  # ✅ 使用统一控制器
    var dice_list = [power_dice, agility_dice, intelligence_dice, skill_dice]
    DiceThrowController.register_all_positions(dice_list)

func throw_all_battle_dices():
    is_charging = false
    var charge_ratio = DiceThrowController.get_charge_ratio()  # ✅ 使用统一控制器
    var dice_list = [power_dice, agility_dice, intelligence_dice, skill_dice]
    DiceThrowController.throw_with_charge(dice_list, charge_ratio)  # ✅ 使用统一控制器
```

**删除代码**:
- 删除 `throw_all_dices_with_charge()` 方法
- 删除独立的力度计算
- 删除独立的蓄力计算

### 步骤 3: 重构 attr_dice_test.gd

**修改内容**:
```gdscript
# _input 方法中
func _input(event):
    if event.is_action_pressed("ui_accept") and is_in_initial_state:
        throw_dice()
        is_in_initial_state = false

func throw_dice():
    var dices = attr_dices
    # ✅ 使用统一的普通投掷
    DiceThrowController.throw_normal(dices, 1.0)
```

**可选：添加蓄力功能**:
```gdscript
var is_charging: bool = false

func _input(event):
    if event.is_action_pressed("ui_accept") and is_in_initial_state:
        is_charging = true
        DiceThrowController.start_charge()
    elif event.is_action_released("ui_accept") and is_charging:
        is_charging = false
        var dices = attr_dices
        DiceThrowController.end_charge(dices)
        is_in_initial_state = false
```

## 统一后的优势

1. **代码复用**: 投掷逻辑集中在 DiceThrowController
2. **手感一致**: 所有场景使用相同的投掷参数
3. **易于调优**: 修改一处参数，全局生效
4. **易于维护**: 减少重复代码
5. **功能统一**: 所有场景都有蓄力和震动效果

## 实施顺序

1. ✅ 先在 dice_demo_script.gd 中测试 DiceThrowController
2. ✅ 重构 skill_dice_test.gd
3. ✅ 重构 attr_dice_test.gd
4. ✅ 测试所有场景确保功能正常

## 测试要点

- [ ] 蓄力功能正常
- [ ] 震动效果正常
- [ ] 投掷力度一致
- [ ] 所有骰子都能正常投掷
- [ ] 结果检测正常

---

**创建日期**: 2026-03-18  
**状态**: 待实施
