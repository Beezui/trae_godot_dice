# 动态更换贴图功能核心代码

## 功能概述

动态更换贴图功能允许通过配置文件和场景切换来动态更改骰子的外观，支持基于ID的配置系统和彩色材质作为贴图的替代方案。

## 核心文件结构

```
├── scripts/
│   ├── dice_config.gd     # 骰子配置系统
│   ├── dice_face_manager.gd # 骰子面管理器
│   ├── dice_csv_reader.gd # 骰子CSV配置读取器
├── scenes/
│   ├── dice_6.gd       # 骰子核心逻辑
│   ├── dice_demo_script.gd # 演示场景脚本
│   ├── dice_manager.gd # 骰子管理器
├── table/
│   ├── NumDices.csv    # 数字骰子配置文件
│   └── 骰子面配置.csv    # 骰子面配置文件
```

**特别说明**：
- 本系统使用动态创建的具有6个独立表面的骰子模型，支持每个面独立更换贴图
- 不再依赖外部3D模型文件，确保每个面都能独立设置材质

## 核心代码分析

### 1. dice_config.gd

**功能**：管理不同场景的骰子配置，支持通过ID配置骰子面。

**核心代码**：

```gdscript
class_name DiceConfig

var dice_face_manager

# 不同场景的骰子贴图配置（使用ID）
var scene_configs = {
	"normal": {
		0: 1,  # 1点
		1: 2,  # 2点
		2: 3,  # 3点
		3: 4,  # 4点
		4: 4,  # 5点
		5: 4   # 6点
	},
	"skill": {
		0: 7,  # 临时使用默认贴图，后续替换为技能贴图
		1: 8,
		2: 9,
		3: 7,
		4: 8,
		5: 9
	}
}

func _init():
	# 初始化骰子面管理器
	dice_face_manager = preload("res://scripts/dice_face_manager.gd").new()

# 获取场景配置（返回ID）
func get_scene_config(scene_name: String) -> Dictionary:
	var config = scene_configs.get(scene_name, scene_configs.normal)
	# 直接返回ID配置，不转换为贴图路径
	return config

# 添加或更新场景配置
func add_scene_config(scene_name: String, config: Dictionary):
	scene_configs[scene_name] = config

# 获取所有场景名称
func get_all_scene_names() -> Array:
	return scene_configs.keys()

# 重新加载骰子面数据
func reload_face_data():
	dice_face_manager.reload()
```

**关键点**：
- 使用字典存储不同场景的配置
- 支持通过场景名称获取配置
- 直接返回ID配置，便于后续处理

### 2. dice_face_manager.gd

**功能**：从CSV文件加载骰子面数据，管理骰子面ID和贴图路径的映射。

**核心代码**：

```gdscript
class_name DiceFaceManager

var face_data = {}
var csv_path = "res://table/骰子面配置.csv"

func _init():
	load_csv()

func load_csv():
	# 加载CSV文件
	print("Loading CSV file from: " + csv_path)
	var file = FileAccess.open(csv_path, FileAccess.READ)
	if file:
		print("CSV file opened successfully")
		# 读取表头
		var header = file.get_line()
		print("CSV header: " + header)
		
		# 读取数据行
		while not file.eof_reached():
			var line = file.get_line()
			if line.is_empty():
				continue
			
			var data = line.split(",")
			print("CSV line: " + str(data))
			if data.size() >= 3:
				var id = int(data[0])
				var path = data[1]
				var description = data[2]
				
				# 构建完整的资源路径，确保使用正斜杠
				var full_path = "res://textures/" + path.replace("\\", "/")
				
				face_data[id] = {
					"path": full_path,
					"description": description
				}
				print("Added face data: ID=" + str(id) + ", Path=" + full_path + ", Desc=" + description)
				
		file.close()
		print("Loaded dice face data: " + str(face_data))
	else:
		print("Failed to load CSV file: " + csv_path)

func get_face_by_id(id: int) -> Dictionary:
	# 根据ID获取骰子面数据
	return face_data.get(id, {})

func get_texture_path_by_id(id: int) -> String:
	# 根据ID获取贴图路径
	var face = get_face_by_id(id)
	return face.get("path", "")

func get_description_by_id(id: int) -> String:
	# 根据ID获取描述
	var face = get_face_by_id(id)
	return face.get("description", "")

func get_all_face_ids() -> Array:
	# 获取所有骰子面ID
	return face_data.keys()

func reload():
	# 重新加载CSV文件
	face_data.clear()
	load_csv()
```

**关键点**：
- 从CSV文件加载骰子面数据
- 构建完整的资源路径
- 提供根据ID获取贴图路径的功能

### 3. dice_6.gd (动态贴图部分)

**功能**：实现骰子的动态贴图和材质管理，包括基于CSV的贴图加载和每个面独立设置材质。

**核心代码**：

```gdscript
@export var dice_face_config: Dictionary = {}
var dice_face_manager

func _ready():
	# 初始化骰子面管理器
	dice_face_manager = preload("res://scripts/dice_face_manager.gd").new()
	
	# 其他初始化代码...

func apply_dice_textures(mesh_instance):
	# 为骰子的六个面应用不同的材质
	var materials = []
	
	# 定义不同ID对应的颜色（作为备用）
	var id_colors = {
		1: Color(1, 0, 0, 1),   # 红色
		2: Color(0, 1, 0, 1),   # 绿色
		3: Color(0, 0, 1, 1),   # 蓝色
		4: Color(1, 1, 0, 1),   # 黄色
		5: Color(1, 0, 1, 1),   # 紫色
		6: Color(0, 1, 1, 1),   # 青色
		7: Color(1, 0.5, 0, 1), # 橙色
		8: Color(0.5, 0, 0.5, 1), # 深紫色
		9: Color(0, 0.5, 0.5, 1)  # 深青色
	}
	
	# 创建六个面的材质
	for i in range(6):
		# 获取当前面的配置
		var face_id = dice_face_config.get(i, 1)
		# 创建材质
		var material = StandardMaterial3D.new()
		material.roughness = 0.8
		
		# 尝试从CSV加载贴图
		var texture_path = dice_face_manager.get_texture_path_by_id(face_id)
		if texture_path and not texture_path.is_empty():
			print("Loading texture for face ", i, " with ID: ", face_id, " from path: ", texture_path)
			var texture = load(texture_path)
			if texture:
				print("Successfully loaded texture: ", texture_path)
				material.albedo_texture = texture
			else:
				print("Failed to load texture: ", texture_path)
				# 加载失败，使用彩色材质
				var color = id_colors.get(face_id, Color(0.5, 0.5, 0.5, 1))  # 默认灰色
				material.albedo_color = color
				print("Using fallback color: ", color)
		else:
			# 没有贴图路径，使用彩色材质
			var color = id_colors.get(face_id, Color(0.5, 0.5, 0.5, 1))  # 默认灰色
			material.albedo_color = color
			print("No texture path, using color: ", color)
		
		materials.append(material)
	
	# 应用材质到网格
	if mesh_instance.mesh and materials.size() > 0:
		# 尝试为每个面设置不同的材质
		var surface_count = mesh_instance.mesh.get_surface_count()
		print("Mesh surface count: ", surface_count)
		
		# 对于多面体网格，为每个面设置材质
		if surface_count >= 6:
			for i in range(6):
				if i < surface_count:
					mesh_instance.mesh.surface_set_material(i, materials[i])
					print("Applied material ", i, " to surface ", i)
			print("Applied materials to mesh surfaces")
		else:
			# 对于单一表面的网格，使用材质覆盖
			mesh_instance.material_override = materials[0]
			print("Applied default material to dice model")
		
		# 存储材质数组，以便在运行时切换
		mesh_instance.set_meta("dice_materials", materials)

func load_dice_model(mesh_instance):
	# 直接使用fallback mesh，确保有6个独立表面
	print("Using fallback mesh with 6 surfaces")
	create_fallback_mesh()

func create_fallback_mesh():
	# 创建备用立方体网格
	var mesh_instance = $MeshInstance3D
	if not mesh_instance:
		mesh_instance = MeshInstance3D.new()
		mesh_instance.name = "MeshInstance3D"
		add_child(mesh_instance)
	
	# 创建一个具有6个独立表面的立方体
	var mesh = ArrayMesh.new()
	
	# 定义立方体的8个顶点
	var vertices = [
		Vector3(-0.5, -0.5, -0.5),  # 0
		Vector3(0.5, -0.5, -0.5),   # 1
		Vector3(0.5, 0.5, -0.5),    # 2
		Vector3(-0.5, 0.5, -0.5),   # 3
		Vector3(-0.5, -0.5, 0.5),   # 4
		Vector3(0.5, -0.5, 0.5),    # 5
		Vector3(0.5, 0.5, 0.5),     # 6
		Vector3(-0.5, 0.5, 0.5)     # 7
	]
	
	# 定义6个面的索引（每个面4个顶点）
	var faces = [
		[0, 1, 2, 3],  # 前面
		[5, 4, 7, 6],  # 后面
		[4, 0, 3, 7],  # 左面
		[1, 5, 6, 2],  # 右面
		[3, 2, 6, 7],  # 顶面
		[4, 5, 1, 0]   # 底面
	]
	
	# 为每个面创建独立的表面
	for i in range(6):
		# 创建表面数据
		var arrays = []
		arrays.resize(Mesh.ARRAY_MAX)
		
		# 顶点数据
		var surface_vertices = []
		for j in faces[i]:
			surface_vertices.append(vertices[j])
		arrays[Mesh.ARRAY_VERTEX] = PackedVector3Array(surface_vertices)
		
		# 法线数据
		var normals = []
		var normal = Vector3(0, 0, 0)
		match i:
			0: normal = Vector3(0, 0, -1)  # 前面
			1: normal = Vector3(0, 0, 1)   # 后面
			2: normal = Vector3(-1, 0, 0)  # 左面
			3: normal = Vector3(1, 0, 0)   # 右面
			4: normal = Vector3(0, 1, 0)   # 顶面
			5: normal = Vector3(0, -1, 0)  # 底面
		for j in range(4):
			normals.append(normal)
		arrays[Mesh.ARRAY_NORMAL] = PackedVector3Array(normals)
		
		# UV数据
		var uvs = [
			Vector2(0, 0),
			Vector2(1, 0),
			Vector2(1, 1),
			Vector2(0, 1)
		]
		arrays[Mesh.ARRAY_TEX_UV] = PackedVector2Array(uvs)
		
		# 索引数据
		var indices = [0, 1, 2, 0, 2, 3]
		arrays[Mesh.ARRAY_INDEX] = PackedInt32Array(indices)
		
		# 添加表面
		mesh.add_surface_from_arrays(Mesh.PRIMITIVE_TRIANGLES, arrays)
	
	# 设置网格
	mesh_instance.mesh = mesh
	
	# 应用贴图
	apply_dice_textures(mesh_instance)
	
	print("Fallback cube mesh with 6 surfaces created")

func set_dice_face_config(config: Dictionary):
	# 设置骰子面的贴图配置
	dice_face_config = config
	# 更新贴图
	update_dice_textures()
	print("Set dice face config: ", config)

func update_dice_textures():
	# 动态更新骰子贴图
	var mesh_instance = $MeshInstance3D
	if mesh_instance:
		apply_dice_textures(mesh_instance)
		print("Updated dice textures")

func _process(delta):
	# 只在需要时更新系统
	if is_rolling:
		# 更新技能冷却（如果方法存在）
		if skill_system and skill_system.has_method("update_cooldowns"):
			skill_system.update_cooldowns(delta)
		# 更新粒子系统（如果方法存在）
		if particle_system and particle_system.has_method("update"):
			particle_system.update(delta)
```

**关键点**：
- 集成了骰子面管理器，支持从CSV加载贴图
- 动态创建具有6个独立表面的骰子模型，确保每个面都能独立设置材质
- 使用ArrayMesh创建自定义网格，每个面作为独立表面
- 支持为每个面设置不同的材质和贴图
- 提供了贴图加载失败时的备用颜色机制
- 不再使用材质覆盖机制，确保每个面显示正确的贴图

### 4. dice_csv_reader.gd

**功能**：骰子CSV配置读取器，从NumDices.csv文件加载数字骰子配置。

**核心代码**：

```gdscript
class_name DiceCSVReader

var num_dices_data = {}

func _init():
	load_num_dices()

func load_num_dices():
	# 加载数字骰子配置
	var csv_path = "res://table/NumDices.csv"
	var file = FileAccess.open(csv_path, FileAccess.READ)
	if file:
		# 读取表头
		var header = file.get_line()
		
		# 读取数据行
		while not file.eof_reached():
			var line = file.get_line()
			if line.is_empty():
				continue
			
			# 解析CSV行，处理带引号的字段
			var data = parse_csv_line(line)
			if data.size() >= 4:
				var id = data[0]
				var face_count = int(data[1])
				var values_str = data[2]
				var textures_str = data[3]
				
				# 解析点数配置
				var values = {}
				var value_array = values_str.split(",")
				for i in range(value_array.size()):
					var value = int(value_array[i])
					values[i] = value
				
				# 解析贴图配置
				var textures = {}
				var texture_array = textures_str.split(",")
				for i in range(texture_array.size()):
					var texture_id = int(texture_array[i])
					# 构建完整的贴图路径，默认前缀为dice_face_
					var texture_name = "dice_face_" + str(texture_id)
					var texture_path = "res://textures/dice/" + texture_name + ".png"
					textures[i] = texture_path
				
				num_dices_data[id] = {
					"face_count": face_count,
					"values": values,
					"textures": textures
				}
				print("Loaded num dice: ", id, " with config: ", num_dices_data[id])
		
		file.close()
		print("Loaded num dices data: ", num_dices_data.keys())
	else:
		print("Failed to load NumDices.csv")

func get_num_dice_config(dice_id: String) -> Dictionary:
	# 获取数字骰子配置
	return num_dices_data.get(dice_id, {})

func get_all_num_dice_ids() -> Array:
	# 获取所有数字骰子ID
	return num_dices_data.keys()

func parse_csv_line(line: String) -> Array:
	# 解析CSV行，处理带引号的字段
	var result = []
	var current_field = ""
	var in_quotes = false
	var i = 0
	
	while i < line.length():
		var char = line[i]
		
		if char == '"':
			in_quotes = !in_quotes
		elif char == ',' and not in_quotes:
			result.append(current_field)
			current_field = ""
		else:
			current_field += char
		
		i += 1
	
	# 添加最后一个字段
	result.append(current_field)
	
	return result

func reload():
	# 重新加载所有配置
	num_dices_data.clear()
	load_num_dices()
```

**关键点**：
- 从NumDices.csv文件加载数字骰子配置
- 支持新的CSV格式，点数和贴图使用逗号分隔的数组
- 贴图默认前缀为dice_face_
- 提供获取骰子配置的功能
- 支持解析带引号的CSV字段

### 5. dice_manager.gd (动态贴图部分)

**功能**：骰子管理器，管理多个骰子实例和配置切换。

**核心代码**：

```gdscript
var dice_csv_reader

func _ready():
	# 加载骰子CSV配置
	dice_csv_reader = preload("res://scripts/dice_csv_reader.gd").new()

func initialize_dice_pool():
	# 清理现有骰子
	for dice in dice_instances:
		if dice and is_instance_valid(dice):
			dice.queue_free()
	dice_instances.clear()
	
	# 限制骰子数量
	dice_count = clamp(dice_count, 1, max_dice_count)
	
	# 获取当前骰子配置
	var dice_config = dice_csv_reader.get_num_dice_config(current_scene)
	var scene_config = dice_config.get("textures", {})
	var value_config = dice_config.get("values", {})
	
	# 创建骰子实例
	for i in range(dice_count):
		var dice = dice_scene.instantiate()
		if dice:
			dice.name = "Dice_%d" % i
			add_child(dice)
			dice_instances.append(dice)
			# 初始状态：禁用重力，使骰子悬浮
			if dice is RigidBody3D:
				dice.gravity_scale = 0.0
				dice.linear_velocity = Vector3.ZERO
				dice.angular_velocity = Vector3.ZERO
			else:
				print("Error: Dice is not RigidBody3D, cannot set physics properties")
			dice.visible = true
			
			# 应用当前场景配置
			if dice.has_method("set_dice_face_config"):
				dice.set_dice_face_config(scene_config, value_config)

func set_scene_config(scene_name: String):
	# 切换场景配置
	current_scene = scene_name
	# 获取骰子配置
	var dice_config = dice_csv_reader.get_num_dice_config(scene_name)
	var scene_config = dice_config.get("textures", {})
	var value_config = dice_config.get("values", {})
	# 更新所有骰子的贴图配置和点数配置
	for dice in dice_instances:
		if dice and is_instance_valid(dice) and dice.has_method("set_dice_face_config"):
			dice.set_dice_face_config(scene_config, value_config)
	print("Set scene config to: ", scene_name)
```

**关键点**：
- 使用dice_csv_reader加载骰子配置
- 同时传递贴图配置和点数配置
- 支持多个骰子实例的管理

### 6. dice_demo_script.gd (动态贴图部分)

**功能**：集成动态贴图系统，支持场景配置切换。

**核心代码**：

```gdscript
var dice_manager

func initialize_dynamic_textures():
	# 初始化动态贴图系统
	if dice_manager:
		# 设置默认场景配置为 1001
		dice_manager.set_scene_config("1001")
		print("Dynamic textures initialized with 1001 scene config")

func set_scene_config(scene_name: String):
	# 切换场景配置
	if dice_manager:
		dice_manager.set_scene_config(scene_name)
		print("Set scene config to: " + scene_name)

func _input(event):
	# 按1键切换到 1001 场景配置
	if event is InputEventKey and event.pressed and event.keycode == KEY_1:
		set_scene_config("1001")
	# 按2键切换到 1002 场景配置
	if event is InputEventKey and event.pressed and event.keycode == KEY_2:
		set_scene_config("1002")
```

**关键点**：
- 初始化动态贴图系统
- 支持通过键盘按键切换场景配置
- 集成到演示场景中

## 配置文件格式

### 1. 骰子面配置.csv

```csv
id,图片路径,简易描述
1,dice\dice_face_1.png,1点
2,dice\dice_face_2.png,2点
3,dice\dice_face_3.png,3点
4,dice\dice_face_4.png,4点
5,dice\dice_face_5.png,5点
6,dice\dice_face_6.png,6点
```

**说明**：
- `id`：骰子面的唯一标识符
- `图片路径`：相对路径，相对于 `textures/` 目录
- `简易描述`：骰子面的描述信息

### 2. NumDices.csv

```csv
id,face_count,values,textures
1001,6,"1,2,3,4,5,6","1,2,3,4,5,6"
1002,6,"6,6,6,6,6,6","6,6,6,6,6,6"
```

**说明**：
- `id`：骰子的唯一标识符
- `face_count`：骰子的面数
- `values`：骰子各面的点数，以逗号分隔的数组
- `textures`：骰子各面的贴图ID，以逗号分隔的数组，默认前缀为dice_face_

## 使用方法

1. **配置骰子**：
   - 在 `table/NumDices.csv` 文件中添加或修改数字骰子配置
   - 格式：id,face_count,values,textures
   - 其中 values 和 textures 是逗号分隔的数组

2. **运行时切换**：
   - 按 1 键：切换到 1001 场景配置
   - 按 2 键：切换到 1002 场景配置

3. **添加新骰子配置**：
   - 在 `table/NumDices.csv` 文件中添加新的骰子配置行
   - 确保 id 唯一，face_count 正确，values 和 textures 数组长度与 face_count 匹配

## 技术实现要点

1. **基于CSV的配置系统**：
   - 使用NumDices.csv文件存储骰子配置
   - 支持点数和贴图的灵活配置
   - 便于后续扩展和维护

2. **彩色材质替代方案**：
   - 当贴图加载失败时，使用彩色材质作为替代
   - 确保在不同环境下都能正常工作

3. **动态更新机制**：
   - 支持在游戏运行时切换场景配置
   - 实时更新骰子的外观

4. **错误处理**：
   - 当配置不存在时使用默认值
   - 当贴图加载失败时使用备用方案
   - 支持解析带引号的CSV字段

5. **多表面网格技术**：
   - 使用ArrayMesh创建具有6个独立表面的立方体
   - 每个面作为独立表面，支持独立设置材质
   - 不再依赖外部3D模型文件，确保跨平台兼容性

6. **材质应用优化**：
   - 使用surface_set_material方法为每个表面设置独立材质
   - 确保每个骰子面都能显示正确的贴图

7. **点数配置系统**：
   - 支持从CSV文件加载点数配置
   - 骰子结果根据配置显示实际点数
   - 与贴图配置同步更新

## 后续扩展建议

1. **支持更多场景配置**：
   - 添加更多场景配置，如节日主题、特殊活动主题等

2. **实现贴图加载**：
   - 解决贴图加载问题，支持真实贴图

3. **动态添加配置**：
   - 支持在游戏运行时动态添加新的骰子面配置

4. **UI界面**：
   - 添加配置切换的UI界面，方便用户操作

5. **材质效果**：
   - 增强材质效果，如添加光泽、反射等

## 总结

动态更换贴图功能通过基于CSV的配置系统、彩色材质替代方案和多表面网格技术，实现了骰子外观和点数的动态切换。该功能支持通过NumDices.csv配置文件和场景切换来更改骰子的外观和点数，为游戏增加了更多的视觉多样性和扩展性。

**核心特性**：
- 基于CSV的配置系统，支持点数和贴图的灵活配置
- 动态创建具有6个独立表面的骰子模型，支持每个面独立更换贴图
- 从NumDices.csv文件加载骰子配置，支持灵活配置
- 彩色材质作为贴图的替代方案，确保在不同环境下都能正常工作
- 支持在游戏运行时切换场景配置，实时更新骰子外观和点数
- 跨平台兼容性，不依赖外部3D模型文件

**技术创新**：
- 使用ArrayMesh创建自定义多表面网格
- 每个骰子面作为独立表面，支持独立设置材质
- 优化的材质应用机制，确保每个面显示正确的贴图
- 完善的错误处理和备用方案
- 支持解析带引号的CSV字段
- 点数配置与贴图配置同步更新

该功能的实现为游戏提供了更加灵活和可扩展的骰子外观管理系统，玩家可以通过配置文件和场景切换来体验不同风格的骰子，增强了游戏的视觉多样性和可玩性。