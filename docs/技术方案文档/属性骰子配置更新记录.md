# 属性骰子配置更新记录

## 修改日期
2026-03-18

## 修改内容

### 1. 创建属性骰子配置文件
**文件**: `table/attr_dices.json`

**内容**: 基于 hero id: 1 的正式属性骰子配置
- **力量骰子**: [5, 11, 17, 23, 29, 35]
- **敏捷骰子**: [4, 10, 16, 22, 30, 32]
- **智力骰子**: [1, 2, 3, 4, 5, 10]

### 2. 修改 skill_dice_test.gd
**修改位置**: 
- `_setup_attribute_dices()` 方法
- `reset_scene()` 方法

**修改内容**:
- 从临时的数字骰子配置改为使用正式的属性骰子配置
- 使用 `dice_csv_reader.get_attr_dice_config("1")` 获取 hero id: 1 的配置
- 添加配置加载失败的警告日志

### 3. 扩展 dice_csv_reader.gd
**新增方法**:
- `load_attr_dices()` - 加载 attr_dices.json
- `get_attr_dice_config(hero_id: String)` - 获取指定英雄的属性骰子配置
- `get_all_hero_ids()` - 获取所有可用的英雄 ID

**数据结构**:
```json
{
  "attr_dices": {
    "hero_1": {
      "hero_id": "1",
      "hero_name": "测试角色",
      "power": { "values": [...], "textures": {...} },
      "agility": { "values": [...], "textures": {...} },
      "intelligence": { "values": [...], "textures": {...} }
    }
  }
}
```

## 使用方式

### 在场景中使用
```gdscript
# 获取 hero id: 1 的属性骰子配置
var attr_config = dice_csv_reader.get_attr_dice_config("1")

# 分别获取三种属性配置
var power_config = attr_config.get("power", {})
var agility_config = attr_config.get("agility", {})
var intelligence_config = attr_config.get("intelligence", {})

# 应用到骰子
dice.set_dice_face_config(
    power_config.get("textures", {}),
    power_config.get("values", {})
)
```

### 切换到其他英雄
```gdscript
# 修改 hero_id 即可切换不同英雄的属性骰子
var hero_id = "2"  # 改为其他英雄 ID
var attr_config = dice_csv_reader.get_attr_dice_config(hero_id)
```

## 配置文件格式

### 添加新英雄的属性骰子配置
在 `table/attr_dices.json` 中添加：

```json
{
  "attr_dices": {
    "hero_1": {
      "hero_id": "1",
      "hero_name": "测试角色",
      "power": {
        "values": [5, 11, 17, 23, 29, 35],
        "textures": {
          "0": "res://textures/dice/dice_face_1.png",
          "1": "res://textures/dice/dice_face_2.png",
          "2": "res://textures/dice/dice_face_3.png",
          "3": "res://textures/dice/dice_face_4.png",
          "4": "res://textures/dice/dice_face_5.png",
          "5": "res://textures/dice/dice_face_6.png"
        }
      },
      "agility": { ... },
      "intelligence": { ... }
    },
    "hero_2": {
      "hero_id": "2",
      "hero_name": "新英雄",
      "power": { ... },
      "agility": { ... },
      "intelligence": { ... }
    }
  }
}
```

## 验证

### 运行 skill_dice_test 场景
1. 打开 `scenes/skill_dice_test.tscn`
2. 运行场景
3. 检查日志输出：
   ```
   【JSON】开始加载 attr_dices.json，路径：res://table/attr_dices.json
   【JSON】加载属性骰子配置 hero_id=1, hero_name=测试角色
   【JSON】attr_dices.json 加载完成，配置数=1
   【属性骰子】配置 PowerDice 贴图：{...}
   【属性骰子】配置 AgilityDice 贴图：{...}
   【属性骰子】配置 IntelligenceDice 贴图：{...}
   ```

4. 确认三个属性骰子正确显示贴图

## 注意事项

1. **配置文件必须存在**: `table/attr_dices.json` 必须存在且格式正确
2. **hero_id 必须匹配**: 使用的 hero_id 必须在配置文件中存在
3. **贴图路径必须有效**: 配置的贴图路径必须指向有效的资源文件
4. **配置加载失败处理**: 如果配置加载失败，会打印警告但不影响场景运行
5. **values 格式**: JSON 中使用数组格式 `[5, 11, 17, 23, 29, 35]`，代码会自动转换为字典格式 `{"0": 5, "1": 11, ...}`

## Bug 修复

### 问题：属性骰子点数配置格式不正确
**现象**: 属性骰子贴图显示异常，点数配置未正确应用

**原因**: JSON 配置中 `values` 使用数组格式，但骰子需要字典格式

**解决方案**: 在 `dice_csv_reader.gd` 中统一预处理配置数据

**修复方法**:
```gdscript
# dice_csv_reader.gd
func _preprocess_attr_dice_config(hero_data: Dictionary):
    # 遍历三种属性配置（power, agility, intelligence）
    var attrs = ["power", "agility", "intelligence"]
    for attr in attrs:
        if hero_data.has(attr):
            var attr_config = hero_data[attr]
            if attr_config.has("values") and attr_config.get("values") is Array:
                var values_array = attr_config["values"]
                var values_dict = {}
                for i in range(values_array.size()):
                    values_dict[i] = values_array[i]
                attr_config["values"] = values_dict
```

**优势**:
- ✅ **统一管理**: 所有场景不需要重复写转换逻辑
- ✅ **一次修复**: 修改一处，所有场景自动生效
- ✅ **代码简洁**: 场景代码不需要关心配置格式
- ✅ **易于维护**: 配置格式变化只需修改一处

**修复位置**:
- `scripts/dice_csv_reader.gd` - 添加 `_preprocess_attr_dice_config()` 方法
- `scenes/skill_dice_test.gd` - 移除重复的转换逻辑（已简化）

**修复日期**: 2026-03-18

---

## 最佳实践

### 配置与代码分离
- ✅ JSON 配置文件使用简洁的数组格式
- ✅ 代码自动处理格式转换
- ✅ 场景代码直接使用配置，无需关心格式

### 统一管理配置
- ✅ 在 `dice_csv_reader.gd` 中统一加载和预处理
- ✅ 所有场景通过 `get_attr_dice_config()` 获取配置
- ✅ 配置格式变化只需修改一处

## 相关文件

- `table/attr_dices.json` - 属性骰子配置文件
- `scenes/skill_dice_test.gd` - 技能测试场景脚本
- `scripts/dice_csv_reader.gd` - 配置加载器
- `scripts/dice/dice_texture_manager.gd` - 贴图管理器（已支持属性骰子）
- `table/hero.json` - 英雄基础数据（参考）

---

## 修改历史

### 2026-03-18: DiceTextureManager 支持属性骰子

**修改内容**:
1. 还原 `attr_dices.json` 为原始格式（attr_name, points_color, values）
2. 修改 `DiceTextureManager.apply_textures_to_dice()` 支持属性骰子配置
3. 添加 `_apply_attr_dice_textures()` 方法处理属性骰子
4. 添加 `_apply_normal_dice_textures()` 方法处理普通骰子
5. 添加 `_apply_materials_to_mesh()` 方法统一材质应用

**配置格式**:
```json
{
  "attr_dices": {
    "1": {
      "hero_id": "1",
      "hero_name": "测试角色",
      "power": {
        "attr_name": "str",
        "points_color": "#C00000",
        "values": [5, 11, 17, 23, 29, 35]
      },
      "agility": {
        "attr_name": "agi",
        "points_color": "#A9D08E",
        "values": [4, 10, 16, 22, 30, 32]
      },
      "intelligence": {
        "attr_name": "int",
        "points_color": "#8EA9DB",
        "values": [1, 2, 3, 4, 5, 10]
      }
    }
  }
}
```

**贴图管理器识别逻辑**:
```gdscript
# 判断是否是属性骰子配置
var is_attr_dice = config.has("attr_name") and config.has("points_color") and config.has("values")

if is_attr_dice:
    _apply_attr_dice_textures(mesh_instance, config)  # 属性骰子
else:
    _apply_normal_dice_textures(mesh_instance, config)  # 普通骰子
```

**当前实现**:
- ✅ 属性骰子使用动态贴图生成（贴图 + 数值文字）
- ✅ 数值文字颜色使用配置的 `points_color`
- ✅ 静态贴图使用 `dice_face_X.png`
- ✅ 使用 SubViewport 渲染动态贴图

**修改者**: Trae CN

---

### 2026-03-18: 统一配置格式（数组格式）

**修改内容**:
1. 修改 `convert_skill_csv_to_json.py` 使用数组格式生成 JSON
2. 修改 `NumDices.json` 为数组格式（values 和 textures）
3. 添加 `_preprocess_num_dice_config()` 方法自动转换为字典格式
4. 保持向后兼容，场景代码无需修改

**统一后的配置格式**:
```json
// NumDices.json
{
  "num_dices": [
    {
      "id": "1001",
      "face_count": 6,
      "values": [1, 2, 3, 4, 5, 6],
      "textures": [
        "res://textures/dice/dice_face_1.png",
        "res://textures/dice/dice_face_2.png",
        "res://textures/dice/dice_face_3.png",
        "res://textures/dice/dice_face_4.png",
        "res://textures/dice/dice_face_5.png",
        "res://textures/dice/dice_face_6.png"
      ]
    }
  ]
}
```

**自动转换逻辑**:
```gdscript
# dice_csv_reader.gd
func _preprocess_num_dice_config(dice_data: Dictionary):
    # 将数组格式的 values 转换为字典格式
    if dice_data.has("values") and dice_data.get("values") is Array:
        var values_array = dice_data["values"]
        var values_dict = {}
        for i in range(values_array.size()):
            values_dict[i] = values_array[i]
        dice_data["values"] = values_dict
    
    # 将数组格式的 textures 转换为字典格式
    if dice_data.has("textures") and dice_data.get("textures") is Array:
        var textures_array = dice_data["textures"]
        var textures_dict = {}
        for i in range(textures_array.size()):
            textures_dict[i] = textures_array[i]
        dice_data["textures"] = textures_dict
```

**优势**:
- ✅ JSON 配置文件更简洁（数组格式）
- ✅ 代码自动处理格式转换（向后兼容）
- ✅ 场景代码无需修改（使用字典格式）
- ✅ 与属性骰子配置格式保持一致

**修改者**: Trae CN

---

**修改者**: Trae CN  
**状态**: 已完成 ✅
