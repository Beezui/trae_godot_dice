# Godot 项目开发全局规则

## 1. 技术栈
- Godot 4.x
- 主脚本语言：GDScript

## 2. 交付标准（生产级、可直接运行）
- 代码完整：无伪代码、无 `TODO`/省略号/占位假设。
- 若使用 addon，需写明获取来源与安装步骤（AssetLib 名称或复制文件夹）。
- 外部依赖（如网络请求、数据库）在示例中用 mock 实现（内存数据、Timer/信号模拟异步）。
- 给出完整项目结构（`── 文件名 ──` 形式）。
- 给出可复现的运行方式：用 Godot 4.x 打开项目，指定启动场景，描述预期行为。
- 脚本包含错误处理与边界检查。
- UI：使用 Godot 内置 Control 节点；多层嵌套时必须遵守以下可运行标准：
  * 每个 Control/Container 明确设置 anchors（至少包含 `anchor_right` 与 `anchor_bottom` 或使用 preset），不允许子节点依赖 0 尺寸的父节点。
  * 非容器叶子 Control 必须显式设置 `rect_min_size` 或由容器正确分配尺寸，避免面积为 0 而不可见。
  * 任何改变颜色/透明度的操作（`modulate`、`self_modulate`、`modulate.a`）需在注释中标注意图，避免意外透明。
  * 确保 `visible = true`（或脚本中不误改），`mouse_filter` 默认不为 `MOUSE_FILTER_IGNORE` 导致交互失效时需注明。
  * 鼓励使用 Theme/容器/Tween/AnimationPlayer 实现整洁、响应式和动效，必要时支持暗色主题。
- 需求不明确时**先提问澄清**，不猜测。
- 始终用简体中文交互。
- 关键逻辑添加中文注释。
- 未明确要求时，不增删项目代码（包括配置）。
- 不确定的代码逻辑不得凭猜测修改。

## 3. 编码行为准则

### 3.1 取舍原则
优先稳妥正确，其次速度。简单琐事可适当灵活。

### 3.2 编码前先思考
- 开始前明示所有前提假设；有疑惑立刻提问。
- 若需求有多种解读，列出全部选项，不自行选定。
- 有更简洁方案时主动说明，可对不合理需求提出优化建议。
- 遇模糊处立即停手，点明疑点请开发者确认。

### 3.3 极简优先
- 只做需求内的功能，不额外扩展。
- 单次使用的代码不抽象。
- 不设计无要求的灵活性/配置项。
- 不可能发生的场景不做多余异常处理。
- 若能大幅精简（如 200 行压缩到 50 行），必须重构。
**自检**：资深 Godot 开发者会不会觉得这段代码过于臃肿？若是，立即简化。

### 3.4 精准局部修改
- 修改已有代码时，不顺手优化无关代码、格式或导入。
- 不重构原本正常的逻辑。
- 遵循项目现有代码风格。
- 发现无关废弃代码，只备注提醒，不擅自删除。
- 只清理因自身改动而产生的无用变量/导入/函数，不动原有遗留。

### 3.5 目标导向
- 把任务转为可验证目标：
  - “增加校验” → 先写非法参数测试，再实现校验使其通过
  - “修复 Bug” → 先复现再修复
  - “重构” → 保证重构前后行为一致
- 多步骤任务先列计划：
  ```
  1. [步骤] → 验证：[校验项]
  2. [步骤] → 验证：[校验项]
  ```
- 以验收标准闭环，避免模糊说辞。

### 3.6 准则生效标志
改动聚焦、重写减少、疑问提前暴露而非事后返工。

### 3.7 Godot UI 布局防错准则

**严重问题集中处**：多层嵌套、容器混用、锚点缺失、尺寸未定、可见性意外。遵循以下规则**在代码生成阶段就消灭**这些 bug。

#### 布局自检清单（输出代码前必须逐项核对）
1. □ 每个 `Container` 的直接子节点是否已设置 `size_flags_horizontal/vertical`（尤其 `EXPAND` 和 `FILL`）？
2. □ 所有非容器叶子节点（Label、TextureRect 等）是否通过容器分配或 `rect_min_size` 获得了非零尺寸？
3. □ 根/中间 Control 是否设置了完整锚点（如 `anchor_right = 1.0; anchor_bottom = 1.0`）以撑满父级？
4. □ 脚本中动态修改 `rect_size` / `rect_position` 后，是否调用了 `reset_size()` 或容器 `queue_sort()`？
5. □ 是否检查了所有节点的 `visible` 属性未被误置为 `false`，`modulate.a` 不为 0？
6. □ 是否混合使用了绝对定位（`anchor_left=anchor_right` 固定宽）与容器自动布局？若必须混用，是否在注释中说明原因？

#### 容器使用铁律
- **VBox/HBox/GridContainer**：子节点自动排列，**绝不允许**手动修改子节点的 `rect_position`，仅通过 `size_flags` 和 `rect_min_size` 控制。
- **MarginContainer**：仅用于添加内边距，其子节点布局仍由内部容器决定，不得依赖 MarginContainer 自动分配子节点尺寸。
- **ScrollContainer**：子节点（通常是一个 Container）必须设置 **完整锚点**（`anchor_right`、`anchor_bottom`）并开启 `scroll_active`，否则内容无法滚动或显示不全。
- **面板类（Panel/PanelContainer）**：边界不会自动跟随子节点，必要时用 `rect_size` 或 `size_flags` 驱动。

#### 调试辅助规则
- 当节点嵌套 ≥ 3 层时，必须在代码注释中添加**布局树结构示意**，例如：
  ```gdscript
  # 布局：VBox > (Label + HBox > (Button, Button))
  ```
- 如果 UI 需要动态生成/销毁，必须包含 `owner` 或 `add_child` 后的可见性赋值，并提供至少一个 `print_debug()` 调用检查关键节点 `rect_size`（输出前可保留注释，但需注明是调试用途）。

#### 可见性常见陷阱
- `ColorRect` 默认尺寸为 0，即使可见也是空白，必须设置锚点或最小尺寸。
- `TextureRect` 的 `expand` 属性为 `false` 时，纹理按原始大小显示，可能超出或小于可视区，必须配合 `stretch_mode` 使用。
- 使用 `Tween` 或动画修改 `rect_scale` 为 `Vector2.ZERO` 后忘记恢复，导致节点缩为不可见点。
- 面板 `visible = false` 时，其下所有子节点也不可见，检查父链表可见性。
